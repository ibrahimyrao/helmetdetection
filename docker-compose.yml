services:
  helmet-tracker:
    build: .
    container_name: helmet-tracker
    ports:
      - "8083:8000"
    volumes:
      - .:/app
      - ./media:/app/media
      - ./shared_status:/app/shared_status
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G
